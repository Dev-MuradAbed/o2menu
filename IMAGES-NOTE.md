# صور المنيو

**لا توجد نسخة من صور المنيو في هذا المشروع عمداً.**

المصدر الموحّد هو خادم البوت:

```
https://o2bot.onrender.com/menu/shawarma/48.jpg
```

`NEXT_PUBLIC_IMAGE_BASE` في متغيّرات البيئة يوجّه كل مسار `/menu/…` إلى هناك.

## لماذا التوحيد

- **نسخة واحدة**: تغيير صورة يجري في مكان واحد
- **‑81 ميغابايت** من حجم هذا المشروع (41 للصور + 40 لملف `menu.zip` المكرر)
- نشر أسرع، ولا خطر بتر الملفات لتجاوز حدود الحجم

## لو أردت العودة لنسخة محلية

انسخ `public/menu` من مشروع البوت إلى هنا، واحذف `NEXT_PUBLIC_IMAGE_BASE`. الكود يدعم الحالتين.

## شبكة أمان

حتى بلا `NEXT_PUBLIC_IMAGE_BASE`، قاعدة `afterFiles` في `next.config.mjs` تجلب أي صورة ناقصة من البوت — بشرط ضبط `BOT_ORIGIN`.
