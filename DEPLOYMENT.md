# أين تنشر كل جزء؟

## الجواب المختصر

**انشرهما منفصلين، واجعلهما على رابط واحد عبر وسيط.**

هذا يجمع فائدتَي الطريقتين: عزل تشغيلي كامل، ورابط واحد يراه الزبون.

---

## لماذا لا تدمجهما في خدمة واحدة

| السبب | التفصيل |
|---|---|
| **دورة حياة مختلفة** | البوت يحمل جلسة واتساب دائمة. كل إعادة تشغيل تقطع الاتصال وقد تتطلب إعادة ربط. الموقع تعدّله كثيراً — تصميم، عروض، صور. الدمج يعني أن كل تعديل بسيط في الموقع يقطع البوت عن الزبائن. |
| **العزل عند الأعطال** | خطأ في صفحة الموقع لا يجب أن يوقف استقبال الطلبات. |
| **موارد مختلفة** | البوت طويل العمر، ثقيل الذاكرة (Baileys)، حركته قليلة. الموقع بلا حالة، حركته متقطّعة، ويستفيد من شبكة توزيع المحتوى. |
| **البناء** | `next build` يأخذ دقائق ويستهلك ذاكرة. تشغيله على نفس خدمة البوت يخنقها. |

---

## المعمارية الموصى بها

```
┌─────────────────────┐
│  Vercel / Netflify  │  موقع الزبائن (Next.js)
│  menu.o2.ps         │  مجاني · CDN · نشر تلقائي من GitHub
└──────────┬──────────┘
           │  /bot-api/*  ← وسيط (rewrite)
           ▼
┌─────────────────────┐
│  Render             │  البوت + لوحة التحكم
│  o2bot.onrender.com │  يعمل 24/7 · جلسة واتساب
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐     ┌──────────────────────┐
│  Firestore          │     │  Firebase Hosting    │
│  البيانات           │     │  o2bot-b7a51.web.app │
│                     │     │  137 صورة            │
└─────────────────────┘     └──────────────────────┘
```

الزبون يرى نطاقاً واحداً فقط. الباقي داخلي.

---

## الإعداد

### (أ) الوسيط — موصى به

في متغيرات بيئة الموقع (Vercel/Netlify):

```
BOT_ORIGIN=https://o2bot.onrender.com
NEXT_PUBLIC_USE_BOT_PROXY=1
```

`next.config.mjs` يعيد كتابة `/bot-api/*` إلى `https://o2bot.onrender.com/api/*`. الطلب يخرج من خادم الموقع لا من متصفح الزبون.

**المكاسب:**

- لا CORS إطلاقاً — الطلب من نفس النطاق
- رابط Render لا يظهر للزبون في أدوات المطوّر
- تخزين منصة الموقع المؤقت يعمل على الطلب
- تغيير استضافة البوت لاحقاً = تعديل متغيّر واحد

**لاحظ:** `BOT_ORIGIN` بلا `NEXT_PUBLIC_` عمداً — يُقرأ على الخادم فقط ولا يُرسل للمتصفح.

### (ب) اتصال مباشر — أبسط

```
NEXT_PUBLIC_BOT_URL=https://o2bot.onrender.com
```

يعمل فوراً بلا وسيط. عيبه أن رابط Render يظهر للزبون، ويعتمد على ترويسات CORS (مضبوطة بالفعل).

إن ضُبط الاثنان يُفضَّل الوسيط.

---

## أين تنشر الموقع

| المنصة | الملاءمة |
|---|---|
| **Vercel** | الأفضل لـ Next.js — صانعوه. مجاني، نشر تلقائي من GitHub |
| **Netlify** | جيد، والمشروع فيه `@netlify/plugin-nextjs` أصلاً |
| Render | ممكن لكن أبطأ للمواقع الثابتة، وبلا CDN في الخطة المجانية |

لا تضع الموقع على نفس خدمة Render التي تشغّل البوت مهما كان.

---

## مسألة نوم Render

الخطة المجانية توقف الخدمة بعد 15 دقيقة بلا طلبات. أثر ذلك على الموقع:

**أول زبون بعد فترة هدوء** ينتظر 30-60 ثانية لجلب التوفّر. لكن **الموقع لا يتجمّد**: طبقة الدمج تعرض المنيو الثابت فوراً، وتحدّث التوفّر عند وصول الرد.

أي أن الأسوأ هو ظهور صنف مغلق لدقيقة، لا صفحة فارغة.

**ما يخفّف ذلك:**

1. النداء الذاتي داخل البوت كل 12 دقيقة (اضبط `RENDER_EXTERNAL_URL`) — يمنع النوم أصلاً
2. حركة الزبائن نفسها توقظ الخدمة
3. الترقية لخطة مدفوعة تنهي المسألة نهائياً

---

## نطاق مخصّص

لو ملكت نطاقاً مثل `o2restaurant.ps`:

```
o2restaurant.ps            → موقع الزبائن (Vercel)
o2restaurant.ps/bot-api/*  → البوت (عبر الوسيط)
admin.o2restaurant.ps      → لوحة التحكم (Render، نطاق مخصص)
img.o2restaurant.ps        → الصور (Firebase Hosting)
```

كلها مجانية على الخطط الحالية عدا النطاق نفسه.

---

## ملخص القرار

| السؤال | الجواب |
|---|---|
| خدمة واحدة أم اثنتان؟ | **اثنتان** — عزل التشغيل يستحق |
| رابط واحد أم اثنان للزبون؟ | **واحد** — عبر الوسيط |
| أين الموقع؟ | Vercel أو Netlify |
| أين البوت؟ | Render (كما هو) |
| أين الصور؟ | Firebase Hosting (كما هي) |
