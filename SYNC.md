# التزامن بين اللوحة والموقع

## المبدأ

**لوحة التحكم هي المصدر الوحيد.** الموقع يعرض ما فيها: الأقسام والأصناف والأسعار والمكونات والصور وحالة التوفّر. أي تغيير يظهر خلال أقل من دقيقة بلا نشر.

## ما تغيّر

كان `menu-data.ts` مصدراً موازياً يُدمج مع بيانات البوت. الازدواجية سبّبت كل ما مررنا به:

| العرض | السبب |
|---|---|
| قسم جديد لا يظهر | ليس في الملف الثابت |
| صنف محذوف يبقى | الملف الثابت يعيده |
| «القسم غير موجود» عند التحديث | الثابت يُرسم قبل وصول البيانات |
| أسعار قديمة | الثابت يفوز في التعارض |

صار الملف الثابت **احتياطياً أخيراً** لا مصدراً.

## ترتيب المصادر

```
1) بيانات حيّة من البوت          ← الأساس
2) آخر نسخة ناجحة في المتصفح     ← تظهر فوراً عند التحديث
3) menu-data.ts                  ← إن لم يسبق للزائر تحميل شيء والبوت متوقف
```

الطبقة الثانية هي ما يحلّ مشكلة التحديث: الصفحة تُرسم من النسخة المحفوظة **فوراً**، ثم تُحدَّث بهدوء عند وصول الرد. لا وميض ولا «غير موجود».

## التحديث

- عند فتح الصفحة
- كل 45 ثانية أثناء بقائها مفتوحة
- عند العودة إلى التبويب

## اختُبر

| العملية في اللوحة | النتيجة في الموقع |
|---|---|
| إضافة قسم + صنف | ظهرا ✅ |
| إغلاق الصنف | صار «مغلق» ✅ |
| تغيير السعر 60 ← 75 | انعكس ✅ |
| حذف الصنف | اختفى ✅ |
| حذف القسم | اختفى ✅ |

**الحذف صار يعمل** — لم يكن ممكناً قبل التوحيد.

## الصور موحّدة أيضاً

| المصدر | المسار |
|---|---|
| صور المنيو الأصلية | `/menu/…` من خادم البوت |
| مرفوعة من الجهاز | `/api/img/<id>` من قاعدة البوت |
| رابط خارجي | كما هو |

قواعد `rewrites` توجّه الأولين للبوت، فتعمل بلا أي متغيّر بيئة.

## متى تحتاج `menu-data.ts` بعد الآن

لا شيء إلزامي. يبقى شبكة أمان لأول زيارة إن كان البوت متوقفاً. حدّثه إن شئت، أو اتركه — لن يتعارض مع اللوحة.

---

# «لا توجد أصناف» رغم وجود الصنف

## السبب

اعتراض مبكر كان يوقف رسم الصفحة كاملاً:

```js
if (categoryData && !categoryData.items.some((i) => i.active !== false)) {
  return <p>لا توجد أصناف متاحة في هذا القسم حالياً</p>;
}
```

مشكلتان فيه:

1. **يعمل أثناء وصول البيانات** — القسم يأتي من النسخة المحفوظة قبل أصنافه، فيُحكم عليه بالخلوّ لحظياً
2. **يحجب الصفحة كاملاً** — بلا ترويسة ولا تذييل ولا زر رجوع، فتبدو الصفحة معطوبة

## الإصلاح

حُذف الاعتراض. حالة الخلوّ تُعرض **داخل شبكة الأصناف** والصفحة كاملة حولها، وتميّز بين حالتين:

| الحالة | ما يظهر |
|---|---|
| البيانات لم تصل بعد | ⏳ «جاري تحميل الأصناف…» |
| وصلت والقسم فارغ فعلاً | «لا توجد أصناف متوفرة في هذا القسم حالياً» |

## تسلسل الشاشات بعد الإصلاح

```
تحديث مع نسخة محفوظة  → الصفحة كاملة + الأصناف فوراً
أول زيارة أثناء الجلب  → مؤشّر «جاري تحميل القسم…»
قسم فارغ فعلاً         → الصفحة كاملة + رسالة الخلوّ
قسم فارغ أثناء الجلب   → الصفحة كاملة + مؤشّر
البوت متوقف            → «القسم غير موجود» + سبب الاتصال
```

طُبّق في `category/[id]` و`local/[branch]/category/[id]`. وصفحة `local/[branch]/categories` كانت تُرجع `null` — شاشة بيضاء — فصار لها مؤشّر تحميل.

---

# «لا توجد أصناف» مع وجود أصناف

## السبب

الأصناف تُصفّى بالفرع، **الأقسام لا**. فقسم أصنافه في غزة يظهر في الفرع الأوسط بلا أصناف:

```
branch=gaza    القسم في القائمة: نعم  | أصنافه: 1
branch=middle  القسم في القائمة: نعم  | أصنافه: 0   ← هنا الرسالة
```

الصنف موجود فعلاً — لكن في الفرع الآخر.

## الإصلاح

النقطة العامة صارت ترجع لكل قسم عدد أصنافه **في كل فرع**:

```json
{ "id": "mashawe", "count": 0, "counts": { "gaza": 1, "middle": 0 } }
```

والموقع يستعملها ليقول للزبون أين يجده:

> لا توجد أصناف متوفرة في هذا القسم حالياً
> **هذا القسم متوفر في فرع غزة — بدّل الفرع لعرضه.**

## تذكير عند الإضافة

نافذة الصنف في اللوحة فيها ثلاثة خيارات:

| الخيار | يظهر في |
|---|---|
| فرع غزة | غزة فقط |
| الفرع الأوسط | الأوسط فقط |
| **الفرعان معاً** | الاثنين |

اختر «الفرعان معاً» إن كان الصنف يُقدَّم في الفرعين.

---

# تبديل الفرع يُخفي القسم

## السبب

النسخة المحفوظة في المتصفح كانت تُستعمل **بلا حدّ لعمرها**:

```js
const cached = readCache(branch);
if (cached) { setPayload(cached); setStatus("cached"); }   // ← مستقرّة فوراً
```

فعند تبديل الفرع تُعرض نسخة ذلك الفرع كما كانت **قبل تعديلك**، وتُعتبر الحالة «مستقرّة» — فيُحكم بخلوّ القسم أو عدم وجوده قبل وصول البيانات الجديدة.

الخادم كان سليماً طوال الوقت: الصنف بخيار «الفرعان معاً» يظهر في الفرعين (اختُبر).

## الإصلاح

**حدّ ثقة للنسخة المحفوظة — دقيقتان:**

| عمر النسخة | الحالة | يُحكم بالخلوّ؟ |
|---|---|---|
| أقل من دقيقتين | `cached` | ✅ نعم |
| أكثر | `stale` | ❌ ينتظر الجديد |
| لا نسخة | `loading` | ❌ ينتظر |

النسخة القديمة **تُعرض** فوراً (أفضل من شاشة فارغة) لكنها لا تُنهي الانتظار. فلا وميض ولا حكم خاطئ.

**وبصمة `rev` من الخادم** تتغيّر مع أي تعديل على المنيو — إضافة، إغلاق، تغيير سعر، حذف — ويحفظها الموقع مع النسخة ليكشف قِدَمها فوراً.

مفتاح التخزين تغيّر إلى `v3`، فالنسخ القديمة تُهمَل تلقائياً عند أول زيارة بعد التحديث.
