# الحل النهائي — التزامن بين اللوحة والموقع

## ما كان يحدث

خمس طبقات متتالية، كل واحدة كانت تُخفي التي تحتها:

| # | العلة | الإصلاح |
|---|---|---|
| ١ | الموقع له مصدران: `menu-data.ts` + البوت | البوت مصدر وحيد، الملف احتياطي أخير |
| ٢ | صفحة الأقسام تبني قائمتها من ثابتة | تُبنى من المنيو الفعلي |
| ٣ | اعتراضات مبكرة تحكم قبل وصول البيانات | الحكم يتأخّر حتى تستقرّ الحالة |
| ٤ | الأصناف تُصفّى بالفرع والأقسام لا | عدّاد `counts` لكل فرع + رسالة توجّه الزبون |
| ٥ | النسخة المحفوظة تُستعمل بلا حدّ لعمرها | حدّ ثقة دقيقتان + بصمة `rev` |

## المعمارية النهائية

```
لوحة التحكم (المصدر الوحيد)
      │
      ├─► البوت على واتساب        ← فوري
      │
      └─► /api/public/menu
              │
              ▼
        موقع الزبائن
        1) بيانات حيّة            ← الأساس
        2) نسخة محفوظة < دقيقتين  ← عرض فوري عند التحديث
        3) نسخة أقدم              ← تُعرض وتنتظر الجديد
        4) menu-data.ts           ← البوت متوقف
```

## التحقق الآلي

```bash
# شغّل البوت محلياً ثم:
node e2e-test.js 3000
```

يحاكي المتصفح بـ localStorage مقلَّد ويشغّل منطق `lib/live-menu.ts` الحقيقي — لا نسخة مبسّطة — مقابل بوت حيّ.

### النتيجة

```
١) إضافة قسم + صنف «الفرعان معاً»
   فرع غزة        1 صنف: مشاوي مشكلة 60₪
   الفرع الأوسط   1 صنف: مشاوي مشكلة 60₪

٢) تحديث الصفحة (قبل الجلب)
   غزة            1 صنف — فوراً بلا وميض
   الأوسط         1 صنف — فوراً بلا وميض

٣) تبديل الصنف لفرع غزة فقط
   فرع غزة        1 صنف
   الفرع الأوسط   لا أصناف (متوفر في gaza)

٤) تغيير السعر 60 ← 75      1 صنف: 75₪
٥) إغلاق الصنف              لا أصناف
٦) إعادة التفعيل للفرعين    يظهر في الاثنين

٧) نسخة قديمة في المتصفح
   الحالة         stale — تنتظر الجديد
   ما يظهر        الصفحة كاملة + مؤشّر
   بعد الجلب      1 صنف: 75₪

٨) حذف الصنف     لا أصناف
   حذف القسم      القسم غير موجود
```

## متطلبات التشغيل

**خدمة البوت** — إصدار 6.17 فأعلى (بصمة `rev` وعدّاد `counts`).

**خدمة الموقع** — متغيّر واحد إلزامي:

```
BOT_ORIGIN=https://o2bot.onrender.com
```

ثم **Redeploy**.

## إن ظهر خلل مستقبلاً

```bash
node e2e-test.js <منفذ-البوت>
```

الخطوة التي تفشل تحدّد الطبقة: ١-٢ بناء المنيو · ٣-٤ الفروع · ٥-٦ التوفّر · ٧ الذاكرة المؤقتة · ٨ الحذف.

وللفحص من المتصفح: `/live-debug` على الموقع، و`node doctor.js <رابط-البوت>` للخادم.

---

# 🐛 الفرع المختار لا يُحفظ

## العرض

```
لا توجد أصناف متوفرة في هذا القسم حالياً
هذا القسم متوفر في الفرع الأوسط — بدّل الفرع لعرضه.
```

بينما الزبون **يتصفّح الفرع الأوسط أصلاً**.

## السبب

```js
const [selectedBranch, setSelectedBranchState] = useState<string | null>("gaza");
```

سياق الفرع يبدأ بـ `"gaza"` ثابتاً ولا يحفظ الاختيار ولا يستعيده. فكل تحديث للصفحة — أو فتح رابط مباشر — يعيد التصفّح لغزة، بينما يظن الزبون أنه في الفرع الذي اختاره.

هذا يفسّر أيضاً ما ظهر سابقاً من اختفاء الأصناف عند تبديل الفرع: التبديل يعمل في الذاكرة فقط ويُفقد مع أول تحديث.

## الإصلاح

الفرع يُحفظ في المتصفح ويُستعاد:

| الحالة | قبل | بعد |
|---|---|---|
| أول زيارة | gaza | gaza |
| بعد اختيار الأوسط | middle | middle |
| **بعد تحديث الصفحة** | ❌ gaza | ✅ middle |
| قيمة محفوظة تالفة | — | ✅ تُتجاهل، يعود لـ gaza |

والقراءة تُزامَن بعد الترطيب لأن SSR لا يرى `localStorage`.

## وزر انتقال مباشر

بدل مطالبة الزبون بالبحث عن مبدّل الفرع:

```
لا توجد أصناف متوفرة في هذا القسم حالياً
هذا القسم متوفر في الفرع الأوسط

      [ الانتقال إلى الفرع الأوسط ]
```

## مفتاحان متوازيان للفرع

اكتُشف أثناء الإصلاح أن الفرع كان يُخزَّن بمفتاحين مختلفين:

| الموضع | المفتاح |
|---|---|
| سياق الفرع | `o2-selected-branch` |
| صفحة الأقسام | `branch` |
| صفحة اختيار الفرع | `branch` |

فاختيارك من مكان لا يراه المكان الآخر. صار السياق يكتب المفتاحين ويقرأهما، وصفحة اختيار الفرع تمرّ عبره بدل الكتابة المباشرة.

```
أول زيارة        → السياق: gaza    صفحة الأقسام: gaza
بعد اختيار الأوسط → السياق: middle  صفحة الأقسام: middle
مفتاح قديم فقط    → السياق: middle  (توافق مع نسخ سابقة)
```
