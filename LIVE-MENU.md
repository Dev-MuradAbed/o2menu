# ربط المنيو الإلكتروني بلوحة التحكم

عند تفعيل أو إلغاء تفعيل صنف من اللوحة (أو بأمر واتساب `#اغلاق`)، يختفي أو يظهر في هذا الموقع خلال ثوانٍ — بلا نشر ولا تعديل كود.

## التشغيل

أنشئ `.env.local` في جذر المشروع:

```
NEXT_PUBLIC_BOT_URL=https://o2bot.onrender.com
```

ضع رابط خدمتك على Render بلا شرطة في النهاية. ثم `npm run build` وانشر.

**للتجربة محلياً:** `NEXT_PUBLIC_BOT_URL=http://localhost:3000`

## كيف يعمل

`lib/menu-data.ts` يبقى مصدر الأسماء والأوصاف والصور والأحجام. الجديد `lib/live-menu.ts` يجلب حالة التوفّر من البوت ويدمجها:

```
menu-data.ts  (ثابت: الأسماء، الصور، الأوصاف، الأحجام)
       +
/api/public/menu  (حيّ: active + السعر المفرد)
       =
useLiveMenu(branch)  →  منيو جاهز للعرض
```

المطابقة بالقسم + الاسم المطبَّع عربياً، فـ«كنافه» تطابق «كنافة». والأسماء المكررة داخل القسم تُطابَق بالترتيب.

## الأمان

**إن تعذّر الوصول للبوت — لأي سبب: نائم، أو Firebase معطّل، أو لا إنترنت — يعمل الموقع بالمنيو الثابت كما كان تماماً.** لا شاشة بيضاء ولا منيو فارغ. وإن نجح الجلب مرة ثم فشل لاحقاً، تبقى آخر نسخة ناجحة معروضة.

## التحديث

- عند فتح الصفحة
- كل 45 ثانية أثناء بقائها مفتوحة
- عند العودة إلى التبويب (`focus` و`visibilitychange`)

مع تخزين مؤقت 30 ثانية عند الحافة، فتغيير الكاشير يظهر خلال دقيقة على أبعد تقدير دون إثقال الخادم.

## الصفحات المربوطة

```
app/category/[id]/page.tsx
app/categories/page.tsx
app/local/[branch]/category/[id]/page.tsx
app/local/[branch]/categories/page.tsx
```

كلها `"use client"` فالخطاف يعمل فيها مباشرة.

## الاستخدام

```tsx
const { menu, status, lastUpdated, refresh } = useLiveMenu(branch);
const categoryData = menu[categoryId];
```

`status` يأخذ: `off` (لم يُضبط الرابط) · `loading` · `live` · `error`.

لإظهار مؤشر للزبون:

```tsx
{status === "live" && <span className="text-xs text-green-500">التوفّر محدَّث الآن</span>}
```

## ما يُدمج وما لا يُدمج

| الحقل | المصدر |
|---|---|
| `active` | **اللوحة** — هذا هو الغرض |
| `price` (مفرد) | اللوحة |
| `pricePerKg` و`variants` | الملف الثابت |
| الاسم والوصف والصورة | الملف الثابت |

الصنف الموجود في الموقع ولا يعرفه البوت يبقى كما هو — لا يختفي.

## التحقق

افتح في المتصفح:

```
https://o2bot.onrender.com/api/public/menu?branch=gaza
```

يجب أن ترى `{"ok":true,"counts":{"total":116,"active":106},...}`. إن رأيت خطأ CORS فالرابط خاطئ أو الخدمة نائمة.

---

# الصور: لماذا فشل `/_next/image`

```
/_next/image?url=%2Fmenu%2Fshawarma%2F48.jpg  →  400
"The requested resource isn't a valid image"
```

مُحسِّن صور Next يبحث عن `public/menu/shawarma/48.jpg` **على الخادم المنشور**. الخطأ يعني أنه لم يجدها هناك — الملف موجود على جهازك لكنه لم يصل النشر.

**السبب الأشيع:** `public/menu` (41 م.ب) و`public/menu.zip` (40 م.ب) — 81 ميغابايت من الصور. كثير من المنصات ترفض أو تبتر النشر عند هذا الحجم.

تحقّق في مشروعك:

```bash
git ls-files public/menu | wc -l      # يجب أن يكون 137
```

إن كان صفراً فالصور لم تُرفع إلى المستودع أصلاً.

## الحل — طبقتان

**١. شبكة احتياطية تلقائية.** أُضيف وسيط في `next.config.mjs`:

```js
afterFiles: [{ source: '/menu/:path*', destination: `${BOT_ORIGIN}/menu/:path*` }]
```

`afterFiles` يُطبَّق **فقط إن لم يوجد الملف في public**. أي:

- الصورة موجودة محلياً ← تُخدَم محلياً (أسرع)
- الصورة ناقصة ← تُجلب من البوت تلقائياً

اختُبر: صورة غائبة محلياً وصلت من البوت بـ 200 و`image/jpeg` و29 ك.ب.

**٢. `remotePatterns`** صار يسمح لمُحسِّن الصور بجلبها من نطاق البوت ومن `*.web.app`، فلا يرفضها بـ 400.

## نظّف الحجم

```bash
rm public/menu.zip      # 40 م.ب بلا فائدة — نسخة مضغوطة من public/menu
```

هذا وحده ينصّف حجم النشر.

---

# الحل النهائي للصور

## لماذا فشلت الشبكة الاحتياطية

```
/_next/image?url=%2Fmenu%2Fshawarma%2F48.jpg  →  400
```

بينما `https://o2bot.onrender.com/menu/shawarma/48.jpg` يفتح صورة سليمة.

**السبب:** مُحسِّن صور Next للمسارات المحلية يقرأ من `public/` مباشرة على القرص — **ولا يمرّ بقواعد `rewrites`**. فقاعدة `afterFiles` التي تجلب الصور من البوت لا تصله أصلاً.

## الحل — ثلاث طبقات

**١. `unoptimized: true`**

المُحسِّن أُطفئ. صور المنيو مضغوطة أصلاً (900px، جودة 80، 7.8 م.ب للـ137) فلا يضيف شيئاً، ووجوده يخلق نقطة فشل كاملة.

الآن `<Image src="/menu/…">` يصير `<img src="/menu/…">` عادياً، فيمرّ بالمسار الطبيعي وتصله قاعدة `afterFiles`.

**٢. `NEXT_PUBLIC_IMAGE_BASE` — الأضمن**

```
NEXT_PUBLIC_IMAGE_BASE=https://o2bot.onrender.com
```

يحوّل كل مسار نسبي إلى رابط كامل نحو البوت **في المتصفح مباشرة**، بلا اعتماد على إعداد خادم ولا rewrites ولا وجود الصور في `public/`.

`lib/img.ts` يطبّق ذلك، ويصلح المسارات المشوّهة (`public/menu/…` ← `/menu/…`)، ويترك الروابط الكاملة كما هي.

**٣. طُبّق في كل مكان يعرض صورة**

`components/product-card.tsx` · `app/category/[id]` · `app/categories` · `app/local/[branch]/categories` — إضافة إلى الخطّاف الذي يصلح كل أصناف المنيو.

## ما تفعله الآن

في متغيرات بيئة خدمة المنيو:

```
NEXT_PUBLIC_IMAGE_BASE=https://o2bot.onrender.com
```

ثم **أعد النشر** — متغيرات `NEXT_PUBLIC_` تُدمج وقت البناء.

لتعود لصور الموقع لاحقاً (بعد التأكد أن `public/menu` يصل النشر) احذف المتغيّر وأعد النشر.
